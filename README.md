# AI Resume–Job Matching Tool

An NLP-based machine learning project that compares a resume with a job description and returns a match score using cosine similarity.

## Tech Stack
- Python
- Scikit-learn
- NLTK
- NumPy
- TF-IDF Vectorization

## Features
- Text preprocessing (tokenization, stopword removal)
- TF-IDF vectorization
- Cosine similarity scoring
- Command-line interface

## How to Run
```bash
pip install -r requirements.txt
python main.py
